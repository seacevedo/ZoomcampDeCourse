base:
  branch: main
  dbt:
    commands:
    - dbt deps
    - dbt build
  piperider:
    command: piperider run
target:
  branch: test
  dbt:
    commands:
    - dbt deps
    - dbt build
  piperider:
    command: piperider run
